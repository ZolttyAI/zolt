{
  "name": "zolt pre-training",
  "image": "mcr.microsoft.com/vscode/devcontainers/python:1-3.12-bookworm",
  "features": {
    "ghcr.io/astral-sh/devcontainer-features/uv:1": {}
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "postCreateCommand": "uv venv .venv && uv pip install torch --index-url https://download.pytorch.org/whl/cpu --python .venv/bin/python && uv pip install tokenizers datasets pytest einops tqdm --python .venv/bin/python && uv pip install -e . --python .venv/bin/python && mkdir -p data/raw data/filtered data/tokens checkpoints/pretrain checkpoints/debug",
  "remoteEnv": {
    "PYTHONPATH": "${containerWorkspaceFolder}"
  }
}
